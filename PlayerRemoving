local Players = game:GetService("Players")
local PlayerDisconnectedServer = game:GetService("ReplicatedStorage"):WaitForChild("Queueing/Gameplay"):WaitForChild("PlayerDisconnectedServer")

Players.PlayerRemoving:Connect(function(player)
	print(player.Name, "has disconnected!")
	PlayerDisconnectedServer:Fire(player)
end)
